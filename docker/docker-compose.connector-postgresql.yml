services:
  postgresql-receiver:
    image: postgres:latest
    environment:
      POSTGRES_USER: arca
      POSTGRES_PASSWORD: arca
      POSTGRES_DB: arca_test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arca"]
      interval: 2s
      timeout: 5s
      retries: 10
